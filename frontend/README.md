# AmaBaKinaata Enterprise Frontend

Next.js + Tailwind UI for AmaBaKinaata Enterprise.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the dev server:
   ```bash
   npm run dev
   ```

The app runs on `http://localhost:3000` by default.
